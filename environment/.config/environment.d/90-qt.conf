# Make Qt apps (quickshell launcher, etc.) read the icon theme from qt6ct/qt5ct,
# which we set to Tela-circle-dracula. Without a platform theme Qt falls back to
# "hicolor" and most app icons are missing.
QT_QPA_PLATFORMTHEME=qt6ct
